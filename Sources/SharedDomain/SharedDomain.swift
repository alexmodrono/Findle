/// SharedDomain module for Foodle.
/// Contains models, identifiers, state machines, and error types shared across all modules.
public enum SharedDomainVersion {
    public static let current = "1.0.0"
}
